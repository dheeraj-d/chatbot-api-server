services:
  - type: web
    name: chatbot-backend
    runtime: node
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3002
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
